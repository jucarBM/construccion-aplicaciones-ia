# Copiar a .env y completar. El .env nunca se sube: está en .gitignore.

# Clave del proveedor de modelos.
OPENROUTER_API_KEY=sk-or-v1-...
MODELO=openai/gpt-4o-mini

# Clave que su propio servicio exige a quien lo llama.
API_KEY=una-clave-larga-que-invente-usted

# Solo para la rama de rescate si el proveedor no está disponible:
# DEMO_MODE=1
